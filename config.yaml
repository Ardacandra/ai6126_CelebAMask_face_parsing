# CelebAMask Face Parsing Configuration

# Dataset paths
data:
  train:
    images: "data/train/images"
    masks: "data/train/masks"
  val:
    images: "data/val/images"

# Output directory
output:
  dir: "out"
  run_id: "srresnet_v2_test_run"

# Visualization settings
visualization:
  num_samples: 6
  dpi: 150
  figsize_width: 10
  figsize_height_per_sample: 3

# Model training settings
training:
  image_size: 512
  batch_size: 4
  num_epochs: 100
  learning_rate: 0.001
  checkpoint_every: 5
  val_split: 0.2
  split_seed: 42
  patience: 10
  use_class_weights: true  # Handle class imbalance

# Model settings
model:
  name: "srresnet_v2"
  num_classes: 19  # CelebAMask-HQ has 19 classes
  max_params: 1821085
  arch_configs:
    srresnet:
      num_residual_blocks: 8
    srresnet_v2:
      num_residual_blocks: 10
      channels: 90
      decoder_channels: [45, 22]
